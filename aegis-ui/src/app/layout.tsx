import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "Aegis — High-Assurance Procurement Gateway",
  description:
    "AI-powered tender evaluation engine with Dual-Pass Normalization, visual grounding, and an immutable audit ledger for CRPF procurement compliance.",
  keywords: ["CRPF", "tender evaluation", "AI", "procurement", "audit", "government"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col font-sans bg-slate-50 text-slate-900">
        {children}
      </body>
    </html>
  );
}
